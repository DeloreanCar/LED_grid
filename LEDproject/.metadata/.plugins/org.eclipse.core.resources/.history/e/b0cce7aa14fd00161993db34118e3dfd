import pprint

grid_size = 9 #int(input('Enter grid size: '))

grid = [[True]*grid_size for i in range(grid_size)]
pprint.pprint(grid)
#pprint.pprint(
for i in (i = 0; grid_size; i++)
    print(sum(grid[i]))

"""
slice_grid = (grid[2:])
pprint.pprint(slice_grid)
pprint.pprint(sum(slice_grid))"""